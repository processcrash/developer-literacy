# 个人智能体工作台设计方案（V1.38）

> 文档状态：设计稿 · 最后更新：2026-09-11
> 目标读者：个人工作台的使用者与维护者
> V1.37 → V1.38 变更：§1.5 新增「底座选择」——运行时层可用开源 harness（如 DeepSeek Harness）作底座，前提是适配器包装、依赖抽象、可替换
> 说明：本文档记录设计想法与已确认的设计；标注「待定」的内容尚未定稿，实施前需想清楚

## 1. 系统概述

### 1.1 系统定位

本系统是面向个人的 **Agent 工作台（Agent Workbench）**，定位为「任务调度中枢 + 多智能体编排台 + 人机决策界面」三合一：

- **任务中枢**：以项目（Project）为顶层容器组织跨仓库、跨领域的工作，任务是可追踪的最小工作单元；
- **编排台**：统一接入并调度多个智能体（本地 CLI 智能体、远程 Agent 服务、外部 agent 平台），支持并行、接力与协作三种协同模式；
- **人机决策界面**：把「需要人决策」的事件（权限请求、方案选择、待审查产出）从海量 agent 输出中提炼出来——对应任务标记 `awaiting-decision`，卡片置顶 + 红边框，点开即处理。

一句话概括：**以任务状态机为骨架、以 agent 运行为血肉、以看板 + 聊天为界面。**

### 1.2 核心能力

- **多项目多任务**：项目是任务的分组与默认值来源，一张看板混排全部项目的任务，顶部可筛选；
- **看板 + 聊天单页**：六列看板管全局（一眼看到所有任务），点卡片在右侧聊天面板深入单个任务；
- **任务池领取制**：agent 从「待实施」列领取任务，系统推荐、自动领取、人可改；
- **多智能体协同**：Agent 抽象层统一接入本地与远程实例，多任务并行、可交接接力；
- **远程 Agent 服务**：通过 HTTP + token 调用远程 Agent 服务，复用其会话与工作区能力；
- **人机边界管理**：权限确认门、待回答、待审查，人只在必要处介入；
- **可审计**：所有领取、执行、交接、决策动作留痕，可回溯到来源会话。

### 1.3 关键设计原则

- **任务与运行分离**：任务（Task）是稳定的工作单元，运行（Run）是 agent 的一次执行尝试；一个任务可多次运行（失败重派、打回重派、回答后重派），聊天记录归任务、跨运行保留；
- **决策优先**：界面默认把「等你的事」（待回答 / 待审查 / 权限请求）集中呈现，不刷屏 agent 输出；
- **宿主权威（Host-authoritative）**：浏览器动作经 Host 确认后才成为状态，杜绝前端乐观写入导致的状态分叉；
- **能力协商而非硬编码**：agent 以 `capabilities` + `endpoint` + `contract` 声明自身，工作台不假设任何具体 agent 的实现细节；
- **协议站队**：工具接入走 MCP，agent 间委派走 A2A，不自造协议；
- **安全默认关闭**：权限提升、远程接入、定时执行均默认关闭，需显式开启并确认。

### 1.4 能力边界

本系统定位「个人生产力工具」，**不包含**：团队协作与 RBAC 体系（无成员、无审批流、无组织层级）、云端多租户部署、任务依赖的甘特图排程、agent 训练与微调。团队级项目治理应交给 Linear / Height / Plane 等专业工具，本系统只做个人视角的聚合与编排。

### 1.5 定位依据：生态位与自建思路（后续完善的纲领）

**四层生态位**——各厂商给出的答案：

| 层 | 厂商 / 路线 | 本质 |
|----|------------|------|
| 模型层 | OpenAI 强化模型 | "我更强，你们来用" |
| 应用层 | Anthropic（Claude Code 专攻编码） | "一个 app 把事办了" |
| 运行时/平台层 | DeepSeek（harness + 插件生态） | "底座给你们，生态收编" |
| **场景工作流层（本系统的位置）** | 普通开发者自建 | 场景的完整流程 + 约束 + 上下文管理 |

**五条自建思路**（后续完善 aipw 的纲领）：

1. **认领生态位**：不做模型、不做通用编码器，做「流程 + 人机边界 + 上下文不丢」——任务不丢、决策置顶、上下文防丢失是本系统的核心价值；
2. **三层解耦，只写中间一层**：编排层（状态机、任务模型、派发、收件箱）**自己写，是护城河**；运行时层（会话、工作区、权限、审计）复用开源 harness 或轻量自建；能力层（模型/工具/agent）**适配器接入**，可替换；
3. **协议只站队，不自造**：工具走 MCP，agent 间走 A2A，本地驱动用子进程 CLI；
4. **把 AI 当「员工」治理，不当「函数」调用**：任务/运行分离、状态机、权限门、人工决策点、交接包与决策日志。agent 工程的真正难点在流程与上下文，不在调模型；
5. **工程化底线**：适配器可替换（不绑死一家厂商）、事件流可见（Agent Map）、全链路审计、成本计量、本地单用户起步。

**底座选择（运行时层可用开源 harness）**：运行时层（会话 / 工作区 / 权限 / 审计）可选用 DeepSeek Harness 之类开源底座，前提是**适配器包装、依赖抽象**——编排层只依赖会话 / 执行接口，不直接调用底座特有 API，保证可替换（依赖倒置原则）。理论依据：分层架构（运行时是横向通用能力、编排是纵向业务能力，横纵分离）；生态位理论（底座是平台层玩家的主场，普通开发者复用而不自建）；成本结构（基础设施成本摊给开源社区，自己只投差异化成本）。判断标准：**换掉底座，流程与上下文设计一行不变。**

**落地顺序**（小处动手验证）：

1. 先用一个 CLI agent 打通一条流水线（派发 → 干活 → 回收结果）；
2. 再接第二个 agent，验证适配器可替换；
3. 然后逐项补编排能力（三态、决策、串行、Agent Map）。

**结论**：本设计稿即「普通开发者自建 agent 工程」的一次完整实践；后续完善以本节五条为纲领。

## 2. 现状调研与差距分析

### 2.1 市面产品：两派格局

**派系 A：传统任务管理器 + AI 增强**

| 产品 | 做法 | 对本场景的局限 |
|------|------|----------------|
| [Linear](https://linear.app/now/coding-sessions-for-linear-agent) | Issue 为中枢，Agent 作为执行者挂在 issue 上，支持 coding sessions 与 Cursor 后台 agent 集成 | 任务模型优秀，但 agent 运行内部不可见；面向团队，个人使用负担偏重 |
| [Height / Plane](https://openhelm.ai/blog/linear-vs-height-vs-plane-project-management) | 视图体系完善（列表/看板/时间线），AI 做摘要与自动化 | 无 agent 运行时概念，AI 是插件而非一等公民 |

**派系 B：Agent Mission Control（新物种）**

| 产品 | 核心设计 | 值得借鉴 |
|------|---------|---------|
| [Clayrune](https://github.com/clayrune-io/clayrune) | 单一 dashboard 跑多个 agent、跨项目、带调度器、跨会话记忆、local-first | 跨项目聚合视图、跨会话记忆 |
| [Codeman](https://github.com/Ark0N/Codeman) | 自托管 mission control，统一托管多种 CLI agent，实时观察每个 subagent | 多 runtime 统一接入 |
| [Hermes Cockpit](https://github.com/goktugozdem2/hermes-cockpit) | 面向特定 agent 生态的多项目 mission control | 项目分组与轮询 |
| [coord](https://github.com/DmarshalTU/coord) | 面向 agent 的 dev-loop 协调器 | 开发闭环编排 |

**协议层已收敛**：[A2A 协议](https://www.linuxfoundation.org/press/a2a-protocol-surpasses-150-organizations-lands-in-major-cloud-platforms-and-sees-enterprise-production-use-in-first-year)已超 150 家组织采用并进入主流云平台，与 [MCP](https://www.xano.com/blog/unlocking-machine-to-machine-intelligence/) 同归 Agentic AI Foundation。**MCP 管工具接入，A2A 管 agent 委派**——本方案直接采用，不重复造轮子。编排框架（LangChain / AutoGen / CrewAI）按 [现有对比研究](https://zenodo.org/records/19109057)各有侧重，本方案不引入重型框架，仅借鉴其编排抽象。

### 2.2 结论：市场空白

派系 A 管得住任务但看不见 agent 内部；派系 B 看得见 agent 但任务易丢失、缺稳定任务模型。**真正的空白是把两者缝合的中间态**：既有稳定可追踪的任务模型，又能观测与编排 agent 运行。本方案即针对该空白。

对同类开源任务插件的源码调研同样印证：普遍缺项目层、缺「待回答/待审查」这类人机边界状态、无 agent 能力协商、远程接入未在界面暴露——这些缺口正是第 3 章需求的来源。

### 2.3 工程参考：多 Agent 系统的实践要点

以下要点摘自社区实践（[LangGraph + MCP + A2A 工程手册](https://www.freecodecamp.org/news/how-to-build-a-multi-agent-ai-system-with-langgraph-mcp-and-a2a-full-book/)、[All-in-One Agent 平台实践](https://developer.cloud.tencent.cn/article/2655982)），作为后续设计的参考：

**1. 什么时候才值得用多个 agent**

满足以下条件中的两条以上，才值得拆多 agent；否则「单 agent + 好工具」更简单可靠：

- 子任务需要的**工具集不同**；
- **调用模式不同**（一次性结构化输出 vs 多轮工具调用循环）；
- **温度/模型要求不同**（规划要低温，创作稍高）；
- 需要**故障隔离**（一部分失败不影响其他）；
- 需要**独立部署或跨框架协作**。

→ 直接指导本系统的「一步绑多 Agent」设计（§11.11 任一/串行）：绑多个 agent 的步骤，要么是"任一执行"的**同质备选**，要么是"串行执行"的**异构分工**（如写→改→审），是否符合拆分的理由要在绑定时想清楚。

**2. MCP 三原语**

- **Tools**：动作——agent 调用、由服务端执行的函数；
- **Resources**：数据——按 URI 读取的结构化数据，只读；
- **Prompts**：可复用的提示词模板，由工具服务持有。

→ 本系统工具接入走 MCP（§1.3 协议站队）：写文件等动作用 Tools，读文件/上下文用 Resources；协议边界保证工具实现可替换、agent 代码不用改——适配器思路的协议版。

**3. 生产级工程要点（与本系统设计的对应）**

| 社区做法 | 本系统对应 |
|---------|-----------|
| 状态检查点：每节点后持久化，崩溃后断点续跑 | 账本 + Run 模型（§5.1 / §5.2） |
| 人工审批是流程图里的一个节点 | `awaiting-decision` 三态（§5.2） |
| 工具调用循环设 max_iterations 熔断 | 卡死检测（§5.2） |
| 全链路观测与评估 | 动作审计（§5.7）、四期质量指标（§9） |

## 3. 核心功能需求

| 类别 | 功能 | 说明 | 阶段 |
|------|------|------|------|
| 项目与任务 | 项目容器 | 项目绑定仓库/目录/默认 agent 预设与权限，是任务的归属 | 一期 |
| 项目与任务 | 任务状态机 | 每项目一套步骤（默认四步：规划→实施→审查→归档），步骤内三态 `pending` / `doing` / `awaiting-decision` | 一期 |
| 项目与任务 | 自定义步骤 | 每项目一套步骤（增删/改名/排序），默认四步；每步可绑定一个或多个 Agent（按上下文派发） | 一期 |
| 项目与任务 | 任务-运行分离 | 任务可多次运行；聊天记录归任务，跨运行保留；每次重派 = 一次新运行 | 一期 |
| 项目与任务 | 任务池 | 每个步骤就是自己的任务池——任务在当前步骤 `pending`，由该步骤绑定的 agent 派发执行 | 一期 |
| 追踪界面 | 看板 + 聊天单页 | 六列看板管全局，右侧聊天面板管单个任务，共用一个页面 | 一期 |
| 追踪界面 | 关注分区 | 看板按「AI 做 / 人做」分区：绑定 Agent 的步骤 = AI 做（🤖 冷色），没绑定 = 人做（🧑 暖色），动态变化；步骤一排列、不画箭头编号、不做收起（方向见 §11.2） | 一期 |
| 追踪界面 | 摘要优先渲染 | 只呈现结论与关键节点，原始流可展开，不默认全量渲染 | 一期 |
| 追踪界面 | 命令面板 | Cmd/Ctrl+K 新建任务、派发、切项目、跳会话 | 二期 |
| 多智能体 | Agent 注册表 | 声明 `capabilities` / `endpoint` / `contract`，支持启用禁用与健康检查 | 一期 |
| 多智能体 | 任务派发 | 系统根据上下文把任务派发给当前步骤绑定的某个 agent（多绑定不竞争），人可改；一任务一 agent，单 agent 有并发上限 | 一期 |
| 多智能体 | 远程派发 | 通过 HTTP + token 派给远程 Agent 服务 | 二期 |
| 多智能体 | 并行执行 | 多任务并行分派给多 agent，互不阻塞；同一工作区串行 | 二期 |
| 多智能体 | 接力（handoff） | 任务交接：交接包 + 进度快照，跨会话/跨 agent 接续 | 二期 |
| 多智能体 | 委派 | 通过 MCP/A2A 把子任务交给外部 agent | 三期 |
| 人机边界 | 权限确认门 | 权限不够的任务停在当前步骤 `pending` 带 🔒，你确认后才可派发 | 一期 |
| 人机边界 | 决策点推送 | 方案选择、歧义澄清以通知形式置顶，禁止静默等待 | 二期 |
| 治理 | 动作审计 | 领取/执行/交接/决策/权限提升全留痕，含发起方会话 | 一期 |
| 治理 | 实时同步 | 事件推送 + revision snapshot，断线可见时重拉 | 一期 |
| 治理 | 数据自主 | 账本本地持久化，无强制云端上报 | 一期 |

## 4. 系统架构设计

### 4.1 总体架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                         前端（Web 客户端）                         │
│  ┌────────────┐ ┌────────────┐ ┌──────────────┐                        │
│  │ 六列任务看板 │ │ 任务聊天    │ │ Agent 注册表  │                        │
│  │(全项目混排) │ │(每任务一个) │ │(capabilities) │                        │
│  └────────────┘ └────────────┘ └──────────────┘                        │
└─────────────────────────────────┬────────────────────────────────┘
                                  │ 同源 HTTP + SSE（revision 提示，拉快照为准）
┌─────────────────────────────────▼────────────────────────────────┐
│                    Host 服务层（本地常驻服务）                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│  │ 任务服务  │ │ 调度器    │ │ Runner   │ │ 编排器    │                    │
│  │(状态机)  │ │(cron)    │ │(会话执行)│ │(推荐+派发)│                    │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘                    │
│       └────────────┴────────────┴────────────┴────────────┘      │
│                                 │                                │
│                    ┌────────────▼────────────┐                   │
│                    │  权威账本（本地持久化）   │                   │
│                    │  projects / tasks /      │                   │
│                    │  runs / agents / events  │                   │
│                    └──────────────────────────┘                   │
└─────────────────────────────────┬────────────────────────────────┘
                                  │
        ┌─────────────────────────┼─────────────────────────┐
        ▼                         ▼                         ▼
┌───────────────┐      ┌──────────────────┐     ┌──────────────────────┐
│ 本地 CLI 智能体 │      │ 远程 Agent 服务    │     │ 外部 Agent 平台       │
│ (子进程/预设)  │      │ (HTTP + token)   │     │ MCP 工具 / A2A 委派   │
└───────────────┘      └──────────────────┘     └──────────────────────┘
```

### 4.2 组件说明与数据流

**1. 任务创建与项目归属**
用户在「规划中」列新建任务，填写标题、目标、验收标准。信息补齐后（手动拖或系统提示）进入「待实施」。项目提供默认值（默认工作区、默认预设、默认权限），减少每次重复填写。

**2. 领取与执行（任务池模式）**
「待实施」列是公共任务池。编排器为每张卡片计算推荐 agent（能力/负载/工作区冲突/权限），agent 自动领取 → 卡片进「实施中」、创建 Run 记录 → Runner 建立会话并下发 Prompt（含来源声明与交接前言）。人可随时改推荐或手动换 agent。

**3. 提问与待回答**
agent 执行中遇到歧义，显式提问 → 任务进「待回答」，你回答后任务回「待实施」**重新派发**（新一次运行）。你的回答 + 进度快照 + 全部聊天记录随任务保留，新 agent 接手时都能看到。

**4. 审查与归档**
执行成功 → 任务进「待审查」，聊天里给出摘要与变更清单。你点「通过」→ 归档；点「打回」→ 回「待实施」重新派发，打回意见写在聊天里。

**5. 失败与卡死（不设状态）**
执行失败 → 任务退回「待实施」，卡片带红色标记，失败原因留在聊天记录。运行超时无活动 → 聊天里弹「中断 / 继续等待」，不另设列。

**6. 权限确认门**
领取时发现任务所需权限高于默认 → 卡片停在「待实施」带 🔒，聊天里出现 [确认] [拒绝]。确认后卡片才可被领取；拒绝则回「规划中」。任何权限或绑定变更都重新武装确认门。

**7. 聊天面板（人机交互唯一通道）**
一个任务一个聊天：进度消息、摘要卡、决策按钮都在这里。需要人决策的事不设独立收件箱——对应任务的卡片直接标记为 `awaiting-decision`（置顶 + 红边框），点卡片进聊天处理。

**8. 多 agent 并行**
不同任务的 Run 彼此独立，可同时分派给不同 agent；工作台对所有运行中的 Run 做统一监控（阶段、耗时、最近活动、是否卡死）。**同一任务默认串行**，同一工作区的任务不同时执行。

**9. 远程 Agent 服务接入（二期）**
远程服务以 `endpoint`（HTTPS 地址）+ 凭据（token 或反向代理注入）注册进 Agent 注册表。派发时工作台通过远程服务的 HTTP 接口建立会话并下发任务；进度通过远程事件接口订阅或轮询。安全上要求：**显式 Host 白名单 + 服务端注入 token + 来源校验**，默认不开启远程接入。远程服务默认只允许 `read-only` 权限，高权限远程运行需额外确认。

**10. 状态同步**
Host 是唯一权威状态源。客户端动作经 Host 确认后才更新 UI；变更返回完整 revision snapshot，SSE 仅提示 revision 变化与调度/电源事件，重连或页面恢复可见时重新拉取完整快照，避免增量丢失。

## 5. 核心模块详细设计

### 5.0 页面规划（框架已确认，细节待定）

整体三个页面 + 一个浮层：

1. **选择项目页（默认打开）**：项目列表（新建 / 进入 / 归档），点项目进入其工作页；
2. **项目工作页**（日常主页面，支持两种模式切换）：
   - **聊天模式**：与 AI 对话聊需求——"帮我将需求整理成任务放到看板里"，AI 澄清、拆草稿，人确认后任务进看板第一步；
   - **看板模式**：列 = 该项目步骤，三态卡片、任务详情浮层、步骤设置 ⚙（现有设计）；
3. **项目设置**：每个项目一份设置能力——Agent 注册表（条目 + 启动配方 + 健康）、步骤配置（增删/改名/排序、人做/AI 做、绑定、任一/串行）、项目默认值（工作区、权限）。

- **已确认**：聊天/看板用**顶部 Tab 切换**；聊天为**每项目一条持续会话**（历史保留）；Agent 注册表**全局共享一份**（步骤绑定从全局注册表选）；项目设置是**独立设置页**（从选择项目页 ⚙ 进入）。

### 5.1 三层数据模型

```
Project（项目）
  └─ Task（任务：稳定工作单元，有状态机 + 自己的聊天记录）
       └─ Run（运行：agent 的一次执行尝试）
```

**为何必须三层**：任务与运行的生命周期不同——一个任务会被多次运行推进（失败重派、打回重派、回答后重派）。若把二者压成一层（任务直接绑会话），就无法表达"同一任务的历史尝试"与"每次运行的结果"。因此一次运行固定归属一个任务，任务通过 `runIds[]` 保有完整尝试历史；**聊天记录属于任务**，跨运行持续累积。

**Project 的字段职责**：项目持有 `workspacePath`（默认工作区）、`defaultPreset`（默认 agent 预设）、`defaultPermission`（默认权限）、`agentAllowlist`（允许参与的 agent）、`namespace`（如需与其他系统隔离）。任务继承项目默认值，可逐项覆盖。

### 5.2 步骤与任务三态（已确认的新模型）

每个项目一套自定义步骤，**默认模板四步**：

```
规划 ──▶ 实施 ──▶ 审查 ──▶ 归档
```

- 步骤顺序即流程，任务按顺序走，不跳级；
- 步骤是「人做」还是「AI 做」，由它绑没绑 Agent 决定（🧑 暖色 / 🤖 冷色，一排列）；
- 「待回答」不是一个步骤——提问的任务留在所在步骤，标记 `awaiting-decision`。

**每个步骤内的任务有三个子状态**（与步骤正交，任意步骤都适用）：

| 子状态 | 含义 | 视觉 |
|--------|------|------|
| `pending` | 等派发（人做步骤里 = 等你拖走） | 普通卡片 |
| `doing` | 执行中（AI 或人） | 普通卡片 |
| `awaiting-decision` | 等人回答（权限确认 / agent 提问） | **置顶 + 红边框** |

**流转规则**：

1. 建任务 → 进入第一步（规划）的 `pending`；
2. **开工前一致性校验**（§11.8，六项照单收）通过 → 派发；不通过 → 停 `pending` 并提示补齐；
3. **派发**：当前步骤绑定 agent → 系统按上下文派发给其中一个 → `doing`；
4. `doing` 的出口：
   - 干完 → **自动进下一步骤**的 `pending`；
   - 提问 / 要权限 → `awaiting-decision`；你回答/确认 → 回本步骤 `pending` **重新派发**；
   - 失败 → **自动重派最多 3 次**（每次带失败原因）；超过 3 次停 `pending` 标 ❌ 等人；
5. 最后一步（默认「审查」，人做）通过 → 归档；打回 → 回本步骤 `pending`；
6. **人做的步骤**：人**拖卡片到下一步骤**推进；任意步骤可放弃（拖到归档）。

**状态流转的约束**：
- 同一任务同时至多一个活跃 Run，避免并发写同一工作区；
- 每次流转写事件（含触发者：人 / 调度器 / agent / 系统），构成审计链；
- 失败原因随聊天记录保留，重派时作为上下文下发给新 agent；
- 回答后重派、打回后重派、失败重派都会产生新 Run，聊天记录不重置。

### 5.3 看板 + 聊天（页面设计）

页面只有一个，左右两区：

```
┌──────────────────────────────────────────────────────┐
│ 顶部：项目筛选 ▾ ｜ 🔍 搜索 ｜ ⌨ Ctrl+K                     │
├────────────────────────────────────────┬─────────────┤
│ 🧑 规划 ｜ 🤖 实施 ｜ 🧑 审查 ｜ 归档      │ 💬 聊天     │
│ （暖色） （冷色） （暖色） （灰）          │ （点卡片    │
│                                        │  后出现）   │
└────────────────────────────────────────┴─────────────┘
```

**看板（左，主视图）**：列 = 当前项目的步骤（默认四步：规划 / 实施 / 审查 / 归档）。卡片可拖拽换步骤（人做步骤靠拖卡推进）。卡片显示：标题、E/S/T 徽标、项目、阶段、**详细描述（markdown 渲染）**、以及状态标记——🔒 待确认、❌ 失败、⏳ 卡死。**卡片不显示 agent、不显示耗时**——agent 在步骤列头与聊天面板，耗时/进度细节在聊天面板。一列可同时放多张卡片。

**列头规范（已确认）**：步骤名 + 🧑/🤖 图标 + 绑定 Agent 标签 + 任务计数。

**卡片视觉规范（已确认）**：三级用大写字母 **E / S / T** 标记；`awaiting-decision` 卡片**置顶 + 红色边框**；不做卡片级「人做 / AI 在做」指示条（列头图标 + 背景色区分）；**卡片不显示 agent**（agent 在步骤列头与聊天面板展示）；**卡片含详细描述，markdown 格式渲染**；描述**默认折叠**（只露前 1-2 行），点卡片展开全文。

**关注分区（视觉）**：步骤一列接一列横向排列（不画箭头、不加编号）；列头图标区分——人做的步骤 🧑（暖色调背景）、AI 做的步骤 🤖（冷色调背景），跟着「步骤绑没绑 Agent」动态变化。AI 步骤不做收起。归档单列靠后，弱化显示。

> 方向（§11.2）：分区动态划分——绑定 Agent 的步骤 = AI 做（🤖 冷色），没绑定 = 人做（🧑 暖色）。步骤配置变了，视觉自动跟着变。

**聊天（右，任务视图）**：点任意卡片打开，一个任务一个聊天。内容：agent 进度消息、摘要卡（原始输出折叠）、你的指令、以及带按钮的决策消息。底部输入框随时直接指挥 agent。聊天记录永远挂在任务上，重新派发后新 agent 可见全部（作为背景资料）。**面板宽度固定；决策按钮配色：确认 = 蓝、拒绝/打回 = 红。**

**渲染策略**：默认只渲染"结论 + 关键节点"（如"已完成 3 个文件修改，等待测试结果"），需要时展开原始流。**绝不全量渲染 agent 输出**——那是噪声而不是信息。

### 5.4 Agent 抽象层与领取

每个 agent 以三元组声明自身，工作台不假设其实现：

```
capabilities: 能做什么（代码修改 / 文档写作 / 检索 / 长任务 / 是否需要 GPU…）
endpoint:     怎么连（local://cli / https://… / mcp://… / a2a://…）
contract:     输入输出契约（超时、并发上限、权限上限、成本特征）
```

| 接入类型 | endpoint 形态 | 实现要点 |
|---------|--------------|---------|
| 本地 CLI 智能体 | `local://cli` | 本地 Runner 启动子进程会话、应用预设、设权限、队列模式发 Prompt |
| 远程 Agent 服务 | `https://host:port` | token 认证 + Host 白名单；订阅或轮询远程事件获取进度 |
| MCP 工具 | `mcp://server` | 作为工具能力接入，供 agent 调用 |
| 外部 agent | `a2a://agent-card` | 按 A2A 规范发现与委派 |

**推荐与领取（方式 C）**：编排器为每张待实施卡片计算推荐 agent（任务需求 vs capabilities、当前负载、工作区冲突、权限），**推荐结果用于系统派发，不显示在卡片上**（agent 信息在步骤列头与聊天面板）。**人可随时改推荐或手动拖卡换 agent**。匹配失败（没有能干的 agent）→ 卡片留在待实施并标注"无可用 agent"，不静默。

> 注（§11.1，已确认）：自定义步骤后，「领取」改为「按上下文派发」——任务走到某一步，系统根据上下文（任务内容、agent 能力、负载）派发给**该步骤绑定的 agent 之一**（多个绑定不是竞争）；「任务池」按步骤分。

**派发 / 领取规则**：
- 一个任务同一时间只能被一个 agent 执行；
- 一个 agent 同时最多执行 N 个任务（N 在 `contract.maxConcurrency`）；
- 同一工作区的任务不同时被多个 agent 执行（派发时检查冲突）；
- 权限不足 → 🔒 停在当前步骤，确认后才可派发。

### 5.5 协同模式

| 模式 | 适用 | 实现要点 | 阶段 |
|------|------|---------|------|
| **并行** | 多个独立任务同时推进 | 每任务独立 Run，统一监控面板 | 二期 |
| **接力** | 长任务跨会话/跨 agent 接续 | 交接包 + 进度快照 + 来源声明；权限变更重新武装确认门 | 二期 |
| **协作** | 多 agent 推进同一任务（如前端+后端） | 子任务拆分 + 共享上下文 + 写冲突规避（同工作区串行） | 三期 |

**取舍**：先做并行与接力，协作模式推迟——同任务多 agent 写冲突的成本远高于收益，现实中拆分任务更有效。

### 5.6 人机边界设计

| 事件 | 呈现 | 人的动作 |
|------|------|---------|
| 权限提升请求 | 待实施卡片 🔒 + 聊天消息（需要什么权限、为什么） | 确认（解除 🔒）/ 拒绝（回规划中） |
| Agent 提问 | 待回答列 + 聊天消息（可带建议选项） | 回答 → 回待实施重新派发 |
| 产出待审查 | 待审查列 + 聊天摘要 + 变更清单 | 通过（归档）/ 打回（带意见，回待实施） |
| 执行失败 | 待实施红色标记 + 聊天里的失败原因 | 重派 / 改任务 / 放弃 |
| 卡死告警 | 聊天消息（超时无活动） | 中断 / 继续等待 |

原则：**人只在"决策"与"审查"处介入，不做"监工"**。所有请求必须可一键操作，不能要求人去翻日志或敲命令。打回意见与回答内容都写回聊天记录，作为下一次 Run 的上下文。

### 5.7 安全模型

- **同源与鉴权**：本地接口要求同源标记；远程访问必须显式 Host 白名单 + 服务端注入 token；
- **权限默认最小**：任务默认权限 `read-only`，高于默认权限的绑定处于"待确认"（🔒 停在待实施），确认后生效，**任何权限或绑定变更都重新武装确认门**；
- **提示注入防护**：卡片文本（尤其来自 agent 的续接快照）以来源声明模板包裹，明确"未经人工审查"，使接手 agent 保持警惕；快照敏感模式自动脱敏、命令类内容整体拒绝；
- **动作白名单**：动作联合中不含命令、可执行路径、shell 文本或任意参数字段，杜绝把工作台变成远程命令执行入口；
- **审计不可依赖前端断言**：客户端上报的发起方仅作审计元数据，不构成信任边界。

### 5.8 上下文传递与防丢失（已确认）

1. **交接包结构化**（目标 / 进度 / 下一步 / 约束），由上一个 agent 写、工作台校验，不靠人传话；
2. **上下文分层，不堆一份大 prompt**：L0 任务卡（目标+验收标准，稳定）→ L1 快照（进度+下一步，每次流转更新）→ L2 聊天/决策记录（只读历史）→ L3 产出物引用（文件路径 / run id，引用而不复制）；
3. **引用而非复制**：账本是唯一事实来源，上下文里放引用，接手方按需取用，避免多份副本不同步；
4. **决策日志**：每个「需要人决策」的问答都落库，新 agent 自动被告知"这个任务有过哪些决策、为什么"；
5. **校验门兜底**：与 §11.8 联动——开工前校验交接包完整性，缺了就拦住，而不是带着残缺上下文跑；
6. **摘要 + 可回查**：长内容只给结构化摘要，原始流可展开回查，不整段塞进 prompt（防截断）。

### 5.9 运行可视化与辅助功能（已纳入设计，实现机制待讨论）

**A. Agent Map（聊天中的 sub-agent 可视化）**

- 设计：agent 执行产生 sub-agent 时，聊天里插入可折叠状态卡「🤖 N 个 sub agent 在跑」（数量实时更新）；点卡片在面板内**就地展开树状 Agent Map**——主 agent 为根 → sub agents → 各自任务；节点显示名字 / 状态 / 当前任务 / 耗时，点节点看该 agent 详情（聊天 / 产出）。
- 待讨论（实现机制）→ **已确认**：采集方式 = Runner 用结构化输出模式启动 CLI，解析输出流中的 sub-agent 事件（如 `--output-format stream-json`）上报账本；**降级** = 拿不到详情时状态卡照常显示，标注「该 agent 未上报详情」。

**B. 项目聊天自动归档（与看板归档严格区分）**

- 设计：项目聊天会话支持自动归档。触发条件：**7 天无新消息**自动归档；会话关联的任务**全部完结**（进入看板归档步骤）后提前归档；手动归档兜底。归档后进入「历史会话」区（折叠、可搜索、不删除），与新对话严格分开显示。
- 待讨论（实现机制）→ **已确认**：「过时」检测 = Host 每天定时检查 + 任务全完结信号（关联任务全部进入看板归档步骤 → **立即归档**）。
- **已确认**：归档会话**只读**（只能查看和搜索，不可发新消息，因此不存在"新消息复活"问题）；想继续聊 → 手动「取消归档」或开新会话。

**C. Board Agent（看板右下角常驻对话框）**

- 设计：看板右下角浮泡，点击展开对话面板；**每项目一个持续会话**，负责运行并追踪看板任务——可问"哪些在跑、哪个卡住、有没有等我的"；**主动推送**：有任务待决策 / 失败停住时浮泡显示红点提醒。与聊天模式会话分开（聊天模式管拆需求，Board Agent 管运行状态）。
- 待讨论（实现机制）→ **已确认**：Board Agent 是**系统内置实现，不需要配置**（不属于 Agent 注册表条目）；运行方式 = 只读账本 + 对话（问运行情况时，工作台注入当前项目任务摘要，它汇总回答，不改任务）；**红点由 Board Agent（AI）判断**触发，不是简单 Host 规则。

## 6. 数据模型设计

以本地 JSON 账本起步（单用户场景足够，便于迁移与备份），结构如下：

- **projects**：`id` / `name` / `workspacePath` / `defaultPreset` / `defaultPermission` / `agentAllowlist[]` / `namespace` / `archived` / `createdAt` / `updatedAt`

- **tasks**：`id` / `projectId` / `level(epic|story|task)` / `parentTaskId` / `title` / `objective` / `acceptance` / `status(planning|ready|running|awaiting|review|archived)` / `pins{workspace,preset,permission}` / `snapshot{goal,progress,next,frozenAt}` / `handoverPack` / `lockReason` / `failureReason` / `reviewFeedback` / `recommendedAgentIds[]` / `runIds[]` / `chat[]` / `archivedAt` / `createdAt` / `updatedAt`

- **runs**：`id` / `taskId` / `agentId` / `sessionId` / `startedAt` / `endedAt` / `result(succeeded|failed|cancelled)` / `error` / `stage` / `initiatedBy` / `cost{tokens,ms}` / `artifacts[]`

- **agents**：`id` / `kind(local|remote|mcp|a2a)` / `endpoint` / `capabilities[]` / `contract{timeout,maxConcurrency,permissionCeiling}` / `health` / `enabled` / `lastSeenAt`

- **events**（审计流）：`id` / `at` / `actor(human|scheduler|agent|system)` / `actorSessionId` / `kind` / `taskId` / `runId` / `detail` / `revision`

- **调度**：`schedules`：`id` / `taskId` / `cron` / `enabled` / `nextFireAt` / `lastFireAt` / `skipped[]`

**索引与治理建议**：`tasks(projectId,status)` 支撑看板查询；`runs(taskId,startedAt)` 支撑执行历史；`events(at)` 分区/滚动归档；每任务执行历史有界（保留最近 N 条）以控制账本体积与写入成本。

## 7. 技术选型

本系统是独立软件：不依赖任何特定 Agent 平台，各平台只是可接入的 agent 来源。

| 部件 | 选型 | 说明 |
|------|------|------|
| 前端 | Web 单页应用 | 六列看板 + 任务聊天 + Agent 注册表；同源 HTTP + SSE 与本地服务通信；原型先用单文件 HTML |
| 后端 | 本地常驻 Host 服务 | 任务状态机、调度、运行、编排（推荐+派发）；唯一权威状态源 |
| 存储 | 本地 JSON 账本 | 单用户够用，易备份、易迁移；schema 版本化，升级可迁移 |
| Agent 接入 | 适配器（Adapter） | 本地 CLI（子进程会话）、远程 HTTP+token、MCP 工具、A2A 外部 agent；新增平台只需实现一个适配器 |
| 编排 | 自研轻量编排器 | 并行/接力/协作在本系统内实现，不引入 LangChain 等重型框架 |

## 8. 优缺点分析

### 8.1 优点

- **任务不丢**：稳定任务模型 + 六列看板全局视图，避免 agent 输出淹没任务；
- **全局一眼 + 细节深入**：看板管"有哪些任务"，聊天管"这个任务怎么样了"，不用来回切页面；
- **决策优先**：待回答/待审查/权限请求集中呈现，减少"盯着看"的无效时间；
- **异构协同**：Agent 抽象层允许多 runtime 并存，不被单一 agent 实现绑定；
- **本地优先**：账本本地持久化，数据自主、离线可用、无强制上报；
- **安全可审计**：权限确认门 + 注入防护 + 动作白名单 + 全链路留痕。

### 8.2 缺点与挑战

- **单用户假设**：无多租户与权限体系，长期多人使用需引入账号与授权（明确不在本期范围）；
- **并行写冲突**：同工作区多 agent 并行有真实冲突风险，只能靠任务拆分与串行约束缓解，不是纯技术问题；
- **重派成本**：待回答、打回都走重新派发，每次多花 token 与时间；靠快照与聊天记录缓解，代价接受；
- **远程接入的信任成本**：远程 Agent 服务意味着凭据管理与网络暴露面，必须坚持默认关闭 + 显式白名单；
- **抽象层成本**：capabilities/contract 需要每个 agent 正确声明，声明失真会导致派发错配，需健康检查与人工兜底；
- **需要人决策噪声**：若事件提取过于宽松，「需要人决策」的卡片会迅速膨胀成第二个信息洪流，提取规则必须保守（宁少勿多）；
- **长任务进度不可靠**：agent 不主动上报阶段时，只能靠"最近活动时间"推断健康度，存在误判可能。

## 9. 落地路线

| 阶段 | 目标 | 交付 |
|------|------|------|
| **一期（骨架）** | 任务不丢、人能看见、能聊 | 六列看板 + 任务聊天 + 权限确认门 + 任务池领取（本地 agent）+ 自建账本与执行链路 |
| **二期（多智能体）** | 并行与接力 | 多任务并行分派 + 远程 Agent 服务接入 + 交接接力 + 决策点推送 + 命令面板 + 卡死告警 |
| **三期（协同）** | 异构协作 | MCP 工具接入 + A2A 委派 + 同任务多 agent 协作（子任务拆分）+ 跨会话记忆 |
| **四期（增强）** | 效率与洞察 | 成本统计（token/时长归集）+ 运行质量指标（失败率、人工打回率）+ 模板化任务 + 产出归档检索 |

**验收口径**（一期末）：一张看板看到两个项目全部任务（六列各有卡片），点卡片打开聊天，对"待审查"一键通过/打回，对"待回答"回答后卡片自动回待实施，权限卡片 🔒 确认后可被领取，全程无需打开终端、无需翻日志。

## 10. 扩展与优化方向

- **成本与配额**：按项目/agent 归集 token 与时长，设日配额并在超限时转 🔒 待确认；
- **质量反馈闭环**：记录人工打回原因，反向优化任务模板与 agent 推荐策略；
- **跨会话记忆**：任务级长期记忆（决策记录、踩坑、约定），供后续 run 复用；
- **依赖编排**：任务前置依赖（DAG），一个任务完成后自动唤醒后继任务；
- **移动端**：只读 + 决策轻操作（确认/打回/回答），重度操作留在桌面；
- **多用户演进**：若确需协作，引入账号与项目成员授权，届时再评估与 Linear 等工具的同步而非自建团队功能；
- **主题**：当前只做亮色；暗色主题等有需求再加。

## 11. 设计想法池（待定设计）

本章记录正在考虑、尚未定稿的想法。**只记想法与影响，不写实施方案；想清楚之后再落地。**

### 11.1 想法 1：自定义步骤（可配置流水线）——核心规则已确认

- **已确认**：流程步骤不固定为六列，由用户自定义——可增删、改名、排序。例如：规划 → 调研 → 实施 → 测试 → 审查 → 归档。
- **已确认：每个项目一套自己的步骤**（步骤是项目级配置，不是全局一套）。
- **已确认：必须按照顺序走**——任务沿步骤顺序推进，不允许跳级；失败 / 打回仍按 §5.2 退回重来，不跨步骤。
- **已确认：一步可绑定多个 AI Agent，但不是竞争**——由系统**根据上下文**（任务内容、agent 能力、当前负载等）**派发给其中一个**完成；「领取」的说法相应改为「派发」。
- **对现有设计的影响**：
  - 状态机（§5.2）：从固定六态改为「每项目一套的步骤列表」，六列降级为默认模板；
  - 看板（§5.3）：列 = 当前项目的步骤，列数由项目配置决定；
  - 任务池与派发（§5.4）：任务池按步骤分——任务在当前步骤，由系统根据上下文派发给该步骤绑定的 agent 之一；
  - Agent 注册表（§6）：增加「agent ↔ 步骤」的多对多绑定关系；
  - 项目（§6 projects）：增加「步骤配置」。
- **已确认的问题（原待想清楚）**：
  1. 步骤范围：每个项目一套；
  2. 顺序：必须按顺序走，不跳级；
  3. 人机步骤的表达：由想法 2 / 4 承接——按步骤是否绑定 Agent 动态分「人做 / AI 做」，人的介入用三态中的「需要人决策」表达；
  4. 多 agent 关系：按上下文派发给其中一个（非竞争、非并行）。
- **剩余待设计**：步骤配置的界面与数据模型细节。

### 11.2 想法 2：AI 做 / 人做的视觉区分（动态分区）

- **想法**：既然所有步骤都可以接入 AI Agent，那么**没接入 Agent 的步骤就由人向下推进**。看板用视觉样式区分两类步骤：**AI 做的步骤**（绑定了 Agent）与**人做的步骤**（没绑 Agent）。
- **关键点：区分不固定**——跟着「该步骤绑没绑 Agent」动态变化。例如默认四步：实施绑了 Agent → AI 做（🤖 冷色）；规划 / 审查没绑 → 人做（🧑 暖色）。某天给「规划」也绑上 Codex，它就从「人做」变成「AI 做」。
- **已确认（布局）**：步骤**一排列**——一列接一列横向排列，按顺序即可；步骤之间**不画箭头、不加编号**。
- **已确认（区分点）**：列头图标——人做的步骤 🧑、AI 做的步骤 🤖；背景色——人做的步骤暖色调、AI 做的步骤冷色调。
- **已确认（取消收起）**：AI 步骤**不做收起**（原「可收起」取消）。
- **已确认（取消卡片级指示条）**：曾提议的卡片左侧 15px「人做 / AI 在做」指示条**取消**——列头图标 + 背景色已足够区分。
- **状态**：视觉样式部分确认，细节见下。
- **待定/待想清楚的问题**：
  1. AI 做的步骤里，需要人介入的事（权限确认、提问）如何呈现？→ 已由想法 4 回答：任务进入所在步骤的「需要人决策」子状态，并视觉突出。
  2. 人做的步骤往下推进后，任务直接进入下一步的任务池，还是停在原地等确认？→ 已确认：人**拖卡片到下一步骤**，任务直接进入下一步骤的 `pending`。

### 11.3 想法 3：Agent 可设置自己的环境（启动配方）

- **想法**：引入 agent（如 Claude Code）之后，可以设置它的环境变量。一个 Agent 条目 = 一份启动配方：命令 + 工作目录 + 环境变量（如 `ANTHROPIC_API_KEY`、`ANTHROPIC_MODEL`、`CLAUDE_CONFIG_DIR`）+ 配置目录。
- **意义**：同一个 CLI 工具可以注册成多个「环境不一致」的条目（不同账号、不同模型、不同目录），例如「Claude-项目A」「Claude-写文档」；步骤绑定 Agent 时，绑定的是具体条目。
- **对现有设计的影响**：§6 agents 增加 `command` / `env{}` / `configDir` 字段；Runner 启动子进程时按配方注入环境。
- **状态**：待定（该能力已确认需要，具体字段与配置界面待设计）。

### 11.4 想法 4：步骤内任务三态（pending / doing / awaiting-decision）——已确认

- **已确认**：每个步骤中的任务都有三个子状态（与步骤正交，适用于任意步骤）：
  - `pending`：任务卡在这个位置，等待被派发；
  - `doing`：任务正在执行——可能由 AI Agent 执行，也可能由人执行；
  - `awaiting-decision`（需要人决策）：一定是人在回答问题（权限确认、agent 提问）。
- **已确认（概念澄清）**：「待审查」是**步骤**（如默认模板的「审查」步骤），`awaiting-decision` 是**任务状态**，两者正交、无关系——不存在"并入"。
- **已确认（视觉）**：`awaiting-decision` 任务**置顶**，卡片**红色边框**。
- **已确认（流转规则）**：
  1. 步骤干完 → 任务**自动进下一步骤**（下一步骤的 pending），不用人管；
  2. `awaiting-decision` 你回答后 → 回本步骤 **pending**，重新派发（沿袭之前决定）；
  3. 执行失败 → **自动重派 N 次**（N 默认 = 3，每次带失败原因），超过 N 次停在 pending 标 ❌ 等人处理。
- **对现有设计的影响**：§5.2 的「待回答」列消失——提问的任务留在所在步骤，标记 `awaiting-decision` 置顶红框；「待实施 / 实施中」是「实施」步骤的 pending / doing 两种子状态。六列模型需按「步骤 + 三态」重新拆解。
- **已确认（默认模板）**：默认四步 **规划 → 实施 → 审查 → 归档**（每个项目可自定义）。

### 11.5 想法 5：需求追踪（Jira 式层级 + 需求=任务）——已确认

- **已确认**：参考 Jira 的做法——默认把所有任务标记出 **Epic / Story / Task** 三级数据；层级说清楚后，人（或通过 AI）就能查看某个 Epic 的完成进度、追踪某个 Task 的进展情况。
- **已确认：固定三级**——Epic / Story / Task 就这三级，不扩展更多层。
- **已确认：不内置「Epic 进度」算法**——系统只负责把三级分清楚（标记 + 父子链接），不预设"进度 = 子任务占比"之类的计算；需要知道 Epic 进度时，由 **AI 分析其下多个任务的实际情况按需回答**（人问，AI 答）。
- **已确认**：需求与任务是相同的——一个需求就是一个任务；需求没完成或完成得不理想时，再创建另一个任务去完善它（不单设「需求」实体）。
- **调研结论（简）**：
  - 主流工具的共同做法是「一条主线 + 层级拆分 + 全程链接」：Jira 用 Epic→Story→Task 层级，所有评论/关联/状态都挂在追踪单元下（参考 [Jira 层级](https://ones.com/blog/jira-issue-hierarchy-map-epics-stories-tasks-subtasks/)）；
  - AI 场景可借鉴 [checkpoint-gated autonomy（检查点门控）](https://github.com/cloudnirvana/open-patterns/blob/main/patterns/trust-governance/checkpoint-gated-autonomy.md)：每阶段结束过人工检查点再放行——与本设计的「步骤 + 需要人决策」一致。
- **落点**：tasks 增加 `level(epic|story|task)` 字段（固定三级），`parentTaskId` 支撑父子链接；系统不做内置进度计算，进度由 AI 在聊天里按需分析任务数据回答（如「这个 Epic 到哪了」）。
- **开放问题**：无（原两项已确认）。

### 11.6 想法 6：收敛步骤（发散 → 收敛）——已确认

- **已确认（位置）**：收敛放在**「规划」步骤的最后一步完成**——不单设一个「收敛」步骤，而是规划步骤的收尾动作。
- **做法**：规划步骤内 agent 发散的问题 / 想法，在该步骤收尾时汇总成一张「收敛单」，任务进入 `awaiting-decision`，人在聊天里决定：哪些回答、哪些合并、哪些丢弃；收敛完成后任务才进入「实施」。
- **依据**：设计思维中的发散-收敛（Double Diamond，[参考](https://link.springer.com/article/10.1186/s41239-025-00572-8#2)）——先放开收集，再收敛成决策。
- **状态**：已确认（具体「收敛单」的呈现形式待与聊天面板一起细化）。

### 11.7 想法 7：步骤上可见 Agent，且容易引入

- **想法**：每个步骤都可以引入 AI Agent 完成当前步骤的任务；引入要简单（如点击步骤头部 → 添加/选择 Agent）；步骤上要能看到已绑定的 Agent（如步骤头部显示 Agent 标签）。
- **已确认（列头内容）**：步骤名 + 🧑/🤖 图标 + 绑定 Agent 标签 + 任务计数。引入 Agent 的操作入口（添加按钮）仍待定。
- **对现有设计的影响**：与 §11.3 启动配方联动（新增 Agent 条目时可直接绑定到步骤）。
- **状态**：待定。

### 11.8 想法 8：任务开始前的一致性校验——已确认

- **已确认**：每个任务在开始（被派发执行）之前，都要做一次信息一致性校验，不通过就不开工。
- **校验什么（六项，照单全收）**：
  - 目标是否填写；
  - 验收标准是否填写；
  - 与所在步骤是否匹配（该步骤需要的前置产出是否齐了）；
  - 交接包 / 快照是否存在且未过期；
  - 权限与任务要求是否一致（是否需要 🔒 确认）；
  - 该步骤有没有可用的 Agent。
- **不通过怎么办**：任务停在当前步骤的 `pending` 或进入 `awaiting-decision`，而不是带着残缺信息开工。
- **依据**：Scrum 里的 Definition of Ready（DoR）——开始前必须满足的条件清单。

### 11.9 想法 9：防止上下文在传递中丢失

- **问题**：上下文在四处容易丢——步骤之间、agent 之间、重新派发时、以及塞进 prompt 时被截断。
- **思路**：
  1. **交接包结构化**（目标 / 进度 / 下一步 / 约束），由上一个 agent 写、工作台校验，不靠人传话；
  2. **上下文分层，不堆一份大 prompt**：L0 任务卡（目标+验收标准，稳定）→ L1 快照（进度+下一步，每次流转更新）→ L2 聊天/决策记录（只读历史）→ L3 产出物引用（文件路径 / run id，引用而不复制）；
  3. **引用而非复制**：账本是唯一事实来源，上下文里放引用，接手方按需取用，避免多份副本不同步；
  4. **决策日志**：每个「需要人决策」的问答都落库，新 agent 自动被告知"这个任务有过哪些决策、为什么"；
  5. **校验门兜底**：与想法 8 联动——开工前校验交接包完整性，缺了就拦住，而不是带着残缺上下文跑；
  6. **摘要 + 可回查**：长内容只给结构化摘要，原始流可展开回查，不整段塞进 prompt（防截断）。
- **已确认**：六条思路照单全收，转入正文 §5.8。

### 11.10 想法 10：人机分工（人管拆与看，AI 管做）

- **想法**：人负责**拆分任务**（Epic → Story → Task 由人拆）、**追踪任务完成情况**、**汇报整个项目的进度**；AI 负责执行步骤里的具体工作。
- **含义**：人 = 规划者 / 追踪者 / 汇报者，AI = 执行者。系统为人的这三件事提供数据（层级聚合、进度视图、待决策清单），AI 可辅助查询（如"这个 Epic 到哪了"）。
- **已确认（界面体现）**：拆分 = 聊天模式（AI 拆草稿 + 人确认）；追踪 = 看板；汇报 = 聊天里问 AI 进度。

---

## 12. 问题与解决方案记录（决策日志）

本章按讨论顺序记录提出的问题与对应的解决方案。状态：✅ = 已确认定稿；🔶 = 待定（想法已记入 §11，未实施）。

| # | 提出的问题 | 解决方案 / 决策 | 状态 | 落点 |
|---|-----------|----------------|------|------|
| 1 | 页面设计不满意，和聊天页面一样 | 融合成「六列看板 + 右侧任务聊天」单页：看板管全局，聊天管单个任务 | ✅ | §5.3 |
| 2 | 看板不满足查看多个进行中的任务 | 「实施中」列可同时放多张卡片，每张显示 agent / 阶段 / 耗时 | ✅ | §5.3 |
| 3 | 看板列叫什么 | 规划中 / 待实施 / 实施中 / 待回答 / 待审查 / 归档 | ✅ | §5.2 |
| 4 | 哪些 Agent 拿取待实施的任务 | 领取制方式 C：系统推荐 + 自动领取 + 人可改；一任务一 agent、单 agent 并发上限、同目录串行 | ✅ | §5.4 |
| 5 | 待回答后任务去哪 | 回「待实施」重新派发（新一次运行） | ✅ | §5.2 |
| 6 | 失败 / 卡死怎么放 | 不设列：失败退回待实施带红色标记（原因留在聊天）；卡死在聊天弹「中断 / 继续」 | ✅ | §5.2 |
| 7 | 收件箱还要不要 | 先保留「待我处理」入口；后取消（见第 27 条）——`awaiting-decision` 置顶 + 红边框已够醒目 | ✅→取消 | §5.3 |
| 8 | 权限请求放哪 | 卡片停在待实施带 🔒，确认后才可被领取；拒绝回规划中 | ✅ | §4.2-6 |
| 9 | 重新派发后新 agent 能看到什么 | 看到该任务的全部聊天记录（作为背景资料） | ✅ | §5.6 |
| 10 | 人关注 vs AI 关注怎么在视觉上区分，且可选择不看 AI 区 | 动态分区——绑了 Agent 的步骤 = AI 做，没绑 = 人做；布局一排列（无箭头编号）；图标 🧑/🤖 + 背景色（人暖 AI 冷）；**AI 步骤不收起**（原收起取消）；卡片级指示条已取消 | ✅ | §11.2 |
| 11 | 想要自定义步骤，每步可接入一个或多个 AI Agent（实施中→Claude Code、规划中→Codex） | 步骤列表可增删改排；每项目一套自己的步骤；必须按顺序走（不跳级，失败/打回原地重来）；一步绑多个 Agent 不是竞争，由系统根据上下文派发给其中一个 | ✅ | §11.1 |
| 12 | 引入 Claude 后要能设置环境变量；想开多个环境不一致的 Claude | Agent 条目 = 启动配方：命令 + 工作目录 + 环境变量 + 配置目录 | 🔶 | §11.3 |
| 13 | 每一步的任务状态；AI 需人介入时怎么办；「需要人决策」要显眼 | 步骤内三态 pending / doing / 需要人决策；AI 有问题要人确认 → 进「需要人决策」；视觉上置顶 + 卡片左侧提示 | 🔶 | §11.4 |
| 14 | 如何追踪一个需求 | Jira 式 Epic / Story / Task **固定三级**；需求 = 任务（不单设实体）；系统不内置进度算法，人（或 AI）按需分析任务数据得出 Epic 进度、追踪 Task 进展 | ✅ | §11.5 |
| 15 | AI 执行时会发散问题，想要一个步骤把问题/需求收敛 | 设「人做的收敛步骤」：汇总前面步骤发散的问题，人回答 / 合并 / 丢弃后再推进 | 🔶 | §11.6 |
| 16 | 每步都能轻松引入 Agent，且步骤上能看到绑定信息 | 步骤头部显示已绑定 Agent 标签 + 一键添加 | 🔶 | §11.7 |
| 17 | 任务开始前要对信息做一致性校验 | 开工前校验（目标 / 验收 / 前置产出 / 交接包 / 权限 / 可用 agent），不过关拦在 pending 或「需要人决策」 | 🔶 | §11.8 |
| 18 | 如何防止上下文在传递中丢失 | 六条：交接包结构化；上下文分层（任务卡/快照/记录/引用）；引用而非复制；决策日志；校验门兜底；摘要+可回查 | 🔶 | §11.9 |
| 19 | 人和 AI 怎么分工 | 人：拆分任务、追踪完成情况、汇报项目进度；AI：执行步骤里的具体工作 | 🔶 | §11.10 |
| 20 | 卡片左侧要能看出人做还是 AI 在做 | 已取消——列头图标 + 背景色足够区分，卡片不做指示条 | ✅（取消） | §11.2 |
| 21 | 「需要人决策」的英文表达 | 选用 `awaiting-decision` | ✅ | §11.4 |
| 31 | 步骤完成后怎么流转 | 自动进下一步骤（pending），不用人管 | ✅ | §11.4 |
| 32 | awaiting-decision 回答后去哪 | 回本步骤 pending，重新派发 | ✅ | §11.4 |
| 33 | 执行失败怎么处理 | 自动重派 N 次（带失败原因），超 N 次停 pending 标 ❌ 等人 | ✅ | §11.4 |
| 34 | 待审查是否并入 awaiting-decision | 不并入——待审查是步骤，awaiting-decision 是任务状态，两者正交 | ✅ | §11.4 |
| 35 | 默认模板的步骤 | 四步：规划 → 实施 → 审查 → 归档（每项目可自定义） | ✅ | §5.2 |
| 36 | 开工前校验清单 | 六项照单全收（目标/验收标准/前置产出/交接包/权限/可用 agent） | ✅ | §11.8 |
| 37 | 人做步骤怎么推进 | 拖卡片到下一步骤 | ✅ | §11.2 |
| 38 | 失败重派次数 N | 默认 3 次 | ✅ | §11.4 |
| 39 | 收敛步骤放哪 | 放在「规划」步骤的最后——规划收尾时汇总发散问题成收敛单，人回答/合并/丢弃后才进实施 | ✅ | §11.6 |
| 40 | 一步多 Agent 怎么执行 | 提议：步骤加执行模式——任一执行（派一个）/ 串行执行（每个都跑一遍），待定 | 🔶 | §11.11 |
| 41 | 任务想推迟到未来再执行 | 提议：任务设 startAt，未到点停 pending 带 ⏰ 标记，到点自动派发，待定 | 🔶 | §11.12 |
| 42 | 第一步之前怎么和 AI 聊目标 | 做成项目工作页「聊天模式」的功能（不是独立页面）；聊清楚后生成任务草稿放进看板第一步 | ✅（框架） | §11.13 |
| 43 | 页面怎么规划 | 三页：选择项目页（默认）→ 项目工作页（聊天/看板双模式）→ 项目设置（Agent 注册表 + 步骤配置 + 默认值） | ✅（框架） | §5.0 |
| 44 | 页面规划四个细节 | 顶部 Tab 切换模式；每项目一条持续聊天会话（保留历史）；Agent 注册表全局共享；项目设置独立页 | ✅ | §5.0 |
| 45 | 聊天里澄清问多细、草稿放哪 | 自由聊 + 出草稿前三件事门槛（目标/验收/约束缺了补问）；草稿全部进第一步 | ✅ | §11.13 |
| 46 | 串行失败/提问怎么处理 | 只重派当前 Agent，前序成果保留 | ✅ | §11.11 |
| 47 | 串行产出怎么合并 | 逐步累积：每个 Agent 基于前一个的产出继续 | ✅ | §11.11 |
| 48 | 推迟到点后怎么处理 | 解除锁定进排队，等下一个派发周期 | ✅ | §11.12 |
| 49 | 防上下文丢失六条 | 照单全收，转入正文 §5.8 | ✅ | §11.9 |
| 50 | 人机分工的界面体现 | 拆分=聊天模式；追踪=看板；汇报=聊天问 AI | ✅ | §11.10 |
| 51 | 聊天里怎么看到 sub agent | 可折叠状态卡「N 个 sub agent 在跑」→ 展开 Agent Map：树状图、就地展开；事件上报机制待定 | ✅（形态） | §11.14 |
| 52 | 项目聊天自动归档怎么做 | 时间触发（默认 7 天无新消息）+ 关联任务全完结提前归档 + 手动兜底；与看板归档严格区分 | ✅ | §11.15 |
| 53 | 看板右下角常驻对话框 | 右下角浮泡 + 一个持续对话的 Board Agent；主动推送红点提醒（待决策/失败停住）；与聊天模式会话分开 | ✅ | §11.16 |
| 54 | sub-agent 事件怎么采集 | Runner 解析 CLI 结构化输出流上报；拿不到详情 → 状态卡标注「未上报详情」 | ✅ | §11.14 |
| 55 | 归档会话为什么会有新消息 | 归档会话只读（只可查看/搜索，不可发新消息）；想继续聊 → 手动取消归档或开新会话 | ✅ | §11.15 |
| 56 | Board Agent 配置从哪来 | 系统内置实现，无需配置（不属于注册表条目） | ✅ | §11.16 |
| 57 | 浮泡红点按什么规则 | 由 Board Agent（AI）判断触发 | ✅ | §11.16 |
| 22 | 三级（Epic/Story/Task）在卡片上怎么标 | 大写字母 E / S / T 标记 | ✅ | §5.3 |
| 23 | awaiting-decision 卡片怎么突出 | 置顶 + 红色边框 | ✅ | §5.3 |
| 24 | 卡片要不要显示 agent | 不显示——agent 在步骤列头（绑定标签）与聊天面板展示 | ✅ | §5.3 |
| 25 | 卡片要不要显示耗时 | 不显示——耗时/进度细节在聊天面板 | ✅ | §5.3 |
| 26 | 卡片要有详细描述 | 卡片含详细描述，markdown 渲染；默认折叠（露前 1-2 行），点卡片展开全文 | ✅ | §5.3 |
| 27 | 「待我处理」要不要 | 取消——awaiting-decision 卡片置顶 + 红边框已够醒目，不单设入口 | ✅（取消） | §5.3 |
| 28 | 列头放什么 | 步骤名 + 🧑/🤖 + 绑定 Agent 标签 + 任务计数，足够 | ✅ | §5.3 |
| 29 | 聊天面板按钮配色与宽度 | 确认 = 蓝、拒绝/打回 = 红；宽度固定 | ✅ | §5.3 |
| 30 | 主题 | 只做亮色，暗色等有需求再加 | ✅ | §10 |

### 11.11 想法 11：步骤内多 Agent 的执行模式

- **想法**：一个步骤绑定多个 AI Agent 时，可设置执行模式：
  - **任一执行**（默认）：系统按上下文派发给其中一个，干完任务流转到下一个步骤（现行模型）；
  - **串行执行**：每个绑定的 Agent **依次都执行一遍**，全部干完才流转下一步骤（如 A 写 → B 改 → C 审）。
- **对现有设计的影响**：步骤增加 `execMode(any|serial)` 设置；串行时卡片显示进度（如「第 2/3 个 Agent」）；后续 Agent 的输入 = 前一个 Agent 的产出 + 交接包 + 聊天记录。
- **已确认**：
  1. 串行中某个 Agent 失败 / 提问 → **只重派当前 Agent**（前序 Agent 的成果保留，从当前继续）；
  2. 产出**逐步累积**——每个 Agent 基于前一个 Agent 的产出继续干，全部完成后流转下一步骤。

### 11.12 想法 12：任务推迟执行

- **想法**：每个任务支持**推迟到未来某个时间执行**——不是 cron 定时重复，而是一次性延迟派发（如「明天上午再开始」）。
- **做法**：任务设置 `startAt`（可选）；未到时间 → 停在 `pending`，卡片带 ⏰ 标记并显示时间，不可派发。
- **已确认**：到点后**只解除锁定**，任务进入排队，等下一个派发周期派发（不强制立即派发）。

### 11.13 想法 13：目标整理聊天（做成功能，不做页面）

- **已确认**：不做独立页面，做成**项目工作页「聊天模式」里的功能**——在聊天中说"帮我将需求整理成任务放到看板里"，AI 澄清、拆解出 E/S/T 任务草稿，人确认后放进看板第一步。
- **已确认**：项目工作页支持两种模式：**聊天模式**（聊需求 / 整理目标）与**看板模式**（步骤看板），可切换。
- **已确认**：聊天为**每项目一条持续会话**，历史保留。
- **已确认（澄清策略）**：自由聊，但**出草稿前强制校验三件事**——目标、验收标准、约束，缺哪件补问哪件（自然又不漏）。
- **已确认（草稿去向）**：拆出的任务草稿**全部放进第一步**（符合"必须按顺序走"）。

### 11.14 想法 14：聊天中的 sub-agent 展示（Agent Map）

- **想法**：聊天框中需要把 sub agent 描述出来——如「3 个 sub agent 在跑」，点击后展开，可分别查看每个 sub-agent 的信息（即 Agent Map）。
- **初步设计**：agent 产生 sub-agent 时，聊天里插入一个可折叠的状态卡（`🤖 N 个 sub agent 在跑`），随进度实时更新；点卡片 → 展开 Agent Map（树状：主 agent → sub agents → 各自任务），每个节点显示名字 / 状态 / 当前任务 / 耗时；点节点看该 agent 详情（聊天 / 产出）。
- **数据来源**：sub-agent 信息由 Runner 采集 agent 事件流（started / finished / status）并渲染。
- **已确认（形态）**：树状图（主 agent 为根 → sub agents → 各自任务）；**就地展开**（点状态卡在聊天面板内展开，不跳页）。
- **已确认**：采集 = Runner 解析 CLI 结构化输出流（如 `--output-format stream-json`）上报；拿不到详情 → 状态卡照常显示并标注「该 agent 未上报详情」。

### 11.15 想法 15：项目聊天自动归档

- **想法**：项目中的聊天要有**自动归档**功能——对话过一定时间、或内容过时后自动归档。**与任务看板的归档完全不同，严格区分**（命名、存储、界面位置均分开）。
- **初步实现思路**（两个触发条件，可组合）：
  1. 时间触发：会话最后活动超过 N 天（默认 7 天，可配）→ 自动归档；
  2. 过时触发：会话关联的任务全部完结（都进入看板归档步骤）后，再无新消息超过 M 天 → 归档；或定期让轻量模型判断对话是否已过时；
  3. 手动归档兜底。
- **归档后形态**：进入「已归档会话」区（折叠、可搜索），不删除；新对话与之分开显示。
- **已确认**：时间阈值默认 **7 天**无新消息自动归档；会话关联的任务全部完结后提前归档；手动归档兜底。

### 11.16 想法 16：看板右下角常驻 Board Agent 对话框

- **想法**：任务看板**右下角**有一个随时可展开的对话框；**只有一个持续对话**，它是负责运行并追踪看板任务的 AI Agent，可随时问它当前运行任务的情况（哪些在跑、进度、卡住 / 待决策）。
- **初步设计**：右下角浮泡，点击展开成对话面板（可收起）；身份 = 「看板助理」（Board Agent），每项目一条持续会话；它读账本数据（tasks / runs）汇总回答。
- **与聊天模式会话的区别**：聊天模式 = 聊需求、拆任务；Board Agent = 回答运行状态。两个会话分开。
- **已确认**：**主动推送提醒**——有任务需要你决策 / 失败停住时，浮泡显示红点提醒；与聊天模式会话分开（各司其职）。
- **已确认**：Board Agent 是**系统内置实现，无需配置**（不属于注册表条目）；红点由 Board Agent（AI）判断触发。

---

## 附录 A：调研来源

- [Linear：Agent 编码会话](https://linear.app/now/coding-sessions-for-linear-agent)
- [Linear vs Height vs Plane 对比](https://openhelm.ai/blog/linear-vs-height-vs-plane-project-management)
- [Clayrune — 多 Claude Code agent mission control](https://github.com/clayrune-io/clayrune)
- [Codeman — 自托管 AI coding agent mission control](https://github.com/Ark0N/Codeman)
- [Hermes Cockpit — 多项目 mission control](https://github.com/goktugozdem2/hermes-cockpit)
- [coord — AI agent dev-loop 协调器](https://github.com/DmarshalTU/coord)
- [A2A 协议采用现状（Linux Foundation）](https://www.linuxfoundation.org/press/a2a-protocol-surpasses-150-organizations-lands-in-major-cloud-platforms-and-sees-enterprise-production-use-in-first-year)
- [MCP 架构与 agent 间通信](https://www.xano.com/blog/unlocking-machine-to-machine-intelligence/)
- [Agent 编排框架对比（LangChain / AutoGen / CrewAI）](https://zenodo.org/records/19109057)
- [手撸 All-in-One AI Agent 平台（MCP/A2A/Skills 三件套）](https://developer.cloud.tencent.cn/article/2655982)
- [用 LangGraph + MCP + A2A 搭建多 Agent 系统](https://www.freecodecamp.org/news/how-to-build-a-multi-agent-ai-system-with-langgraph-mcp-and-a2a-full-book/)

## 附录 B：术语

| 术语 | 含义 |
|------|------|
| Project | 项目：任务顶层容器，绑定仓库/默认预设/默认权限 |
| Task | 任务：稳定工作单元，持有状态机、执行历史与自己的聊天记录 |
| Epic / Story / Task | 任务的固定三级粒度（借鉴 Jira）：Epic 大目标 → Story 功能块 → Task 具体工作；系统不做内置进度计算，进度由 AI 分析任务数据按需回答 |
| 步骤（Step） | 项目级流水线的一格，每个项目一套自己的步骤；任务必须按顺序经过；可绑定一个或多个 Agent |
| 派发 | 系统根据上下文（任务内容、agent 能力、负载）把任务交给当前步骤绑定的某个 agent |
| Run | 运行：agent 的一次执行尝试，绑定会话与结果 |
| 看板 | 六列任务视图：规划中/待实施/实施中/待回答/待审查/归档，全局一眼看全 |
| 聊天面板 | 每任务一个的对话视图，人机交互的唯一通道 |
| 任务池 | 「待实施」列，agent 领取任务的公共队列 |
| 领取 | agent 从任务池取走任务并开始运行 |
| 推荐 | 编排器为卡片标注的最合适 agent，可被人修改 |
| 交接包 | 跨 agent/会话传递任务时钉住的上下文三元组与引用 |
| 权限确认门 | 高权限运行需人工确认的安全闸门（🔒），变更即重新武装 |
| Host 权威 | 服务端为唯一状态源，客户端动作经确认才生效 |
